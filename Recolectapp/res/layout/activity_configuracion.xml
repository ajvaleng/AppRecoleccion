<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Configuracion" >

    <EditText
        android:id="@+id/tbPatente"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/tbRecorrido"
        android:layout_alignBottom="@+id/tbRecorrido"
        android:layout_alignRight="@+id/tbApellido"
        android:ems="10"
        android:hint="Patente" />

    <EditText
        android:id="@+id/tbNombre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tbApellido"
        android:layout_alignParentTop="true"
        android:layout_marginTop="42dp"
        android:ems="10"
        android:hint="Nombre" />

    <EditText
        android:id="@+id/tbApellido"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tbNombre"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="39dp"
        android:ems="10"
        android:hint="Apellido" />

    <EditText
        android:id="@+id/tbRecorrido"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btEntrar"
        android:layout_alignLeft="@+id/tbApellido"
        android:layout_marginBottom="32dp"
        android:ems="10"
        android:hint="Recorrido" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btEntrar"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="103dp"
        android:text="Entrar" />

</RelativeLayout>